{{page-title "Index"}}
<div>
  <div>
    <h2 class="text-center">
      Welcome sup4 peek team 😎✈️🚀..
    </h2>
  </div>
  {{#each @model as |item|}}
    {{item.id}}-{{item.title}}-{{item.start}}<br />
  {{/each}}
</div>
{{outlet}}
